Процедура ПроверкаПримитива()
    Массив = Новый Массив;
    Массив.Добавить(1);

    Значение = Массив[0];
    Значение = 2;
    Сообщить(Массив[0]);
КонецПроцедуры

Процедура ПроверкаОбъекта()
    Массив = Новый Массив;
    Массив.Добавить(Новый Массив);

    Значение = Массив[0];
    Значение.Добавить(911);
    Значение = Неопределено;

    Сообщить(Массив[0][0]);
КонецПроцедуры
