Высота = 100;

Массив = Новый Массив;
Массив.Добавить(Ложь);
Массив.Добавить(Ложь);

Для Индекс = 2 По Высота Цикл
	Массив.Добавить(Истина);
КонецЦикла;

Для Индекс = 2 По Высота Цикл
	Если Массив[Индекс] Тогда
		Квадрат = Индекс * Индекс;
		Если Квадрат <= Высота Тогда
			Значение = Квадрат;
			Пока Значение <= Высота Цикл
				Массив[Значение] = Ложь;
				Значение = Значение + Индекс;
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
КонецЦикла;